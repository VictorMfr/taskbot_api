{
    "version": 2,
    "builds": [
        {
            "src": "mcp-server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "mcp-server.ts"
        }
    ]
}