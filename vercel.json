{
    "version": 2,
    "builds": [
        {
            "src": "index.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "index.ts"
        },
        {
            "src": "/mcp/(.*)",
            "dest": "/api/mcp"
        }
    ],
    "functions": {
        "index.ts": {
            "runtime": "nodejs18.x"
        }
    }
}